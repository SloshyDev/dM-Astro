query ReportBySlug($slug: String!) {
  dataContents(filters: { Slug: { eq: $slug }, Type: { eq: "Report" } }, pagination: { limit: 1 }) {
    documentId
    Title
    Slug
    Type
    Caption
    PDF {
      url
    }
    SharingContent {
      url
      width
      height
      size
    }
  }
}
